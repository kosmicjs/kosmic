import test from 'node:test';

test('should run tests', () => {
  // this test doesn't need to assert anything, we just need it to run without errors to verify that our test environment is set up correctly
});
