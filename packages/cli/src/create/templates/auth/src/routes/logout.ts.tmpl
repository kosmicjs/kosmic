import type {Context, Next} from '@kosmic/server';

/**
 * Logout the current user and redirect home.
 */
export async function get(ctx: Context, _next: Next) {
  await ctx.logout();

  ctx.redirect('/');
}
